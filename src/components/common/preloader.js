import React from 'react';
import ReactLoading from 'react-loading';

const PreLoader = ({ spokes, red }) => (
  <ReactLoading type="spokes" color="green" height={100} width={100} />
);

export default PreLoader;
